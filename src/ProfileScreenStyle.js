// Style.js

// Bring in StyleSheet abstraction from React Native
import {
    StyleSheet,
} from 'react-native';

export default StylesMain = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
        backgroundColor: "#FF0000",
    },

    content: {
        flex: 1,
        backgroundColor: "black",
    },
});
